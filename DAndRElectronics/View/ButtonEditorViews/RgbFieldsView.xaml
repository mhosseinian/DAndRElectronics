<UserControl x:Class="DAndRElectronics.View.ButtonEditorViews.RgbFieldsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DAndRElectronics.View.ButtonEditorViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="30"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="30"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="30"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Content="R"></Label>
        <TextBox Grid.Column="1" Text="{Binding R, UpdateSourceTrigger=PropertyChanged}" ></TextBox>
        <Label Grid.Column="2" Content="G"></Label>
        <TextBox Grid.Column="3" Text="{Binding G, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <Label Grid.Column="4" Content="B"></Label>
        <TextBox Grid.Column="5" Text="{Binding B, UpdateSourceTrigger=PropertyChanged}"></TextBox>


    </Grid>
</UserControl>
