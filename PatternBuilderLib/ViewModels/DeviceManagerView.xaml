<UserControl x:Class="PatternBuilderLib.ViewModels.DeviceManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Canvas x:Name="CanvasControl" Visibility="{Binding IsLine, Converter={StaticResource InvBoolToVis}}"></Canvas>

        <DockPanel x:Name="DockControl" Visibility="{Binding IsLine, Converter={StaticResource BoolToVis}}"></DockPanel>

    </Grid>
</UserControl>
